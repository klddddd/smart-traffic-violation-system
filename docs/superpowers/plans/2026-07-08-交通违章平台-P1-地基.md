# 交通违章智能管理平台 · Plan 1：地基（M0–M2）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 搭建后端地基——FastAPI 骨架 + 配置 + MySQL/Alembic + JWT/RBAC 鉴权 + 图片接入与本地存储，产出可独立运行、可测试的 API（无 AI 部分，AI 流水线在 Plan 2）。

**Architecture:** 单体 FastAPI 应用，SQLAlchemy 2.0 ORM，Alembic 迁移，JWT+RBAC 鉴权，本地文件存储。测试用 SQLite in-memory，运行时用本地 MySQL 8.x。

**Tech Stack:** Python 3.11、uv、FastAPI、SQLAlchemy 2.0、Alembic、Pydantic v2、pydantic-settings、python-jose、bcrypt、pymysql、python-multipart、pytest、httpx

## Global Constraints

- Python 3.11，包管理用 uv。
- 运行时 DB：本地 MySQL 8.x（`DATABASE_URL` 经 `.env` 配置，驱动 `mysql+pymysql`）；本地 Redis（本 Plan 暂不使用，仅配置占位）。
- 测试 DB：SQLite in-memory（`sqlite:///:memory:`），模型不得使用 MySQL 专属语法（JSON 列用 SQLAlchemy 通用 `JSON` 类型，不用 MySQL 函数）。
- API 统一前缀 `/api/v1`。
- 角色代码：`citizen` / `reviewer` / `admin` / `camera`。
- 案件编号格式：`CASE{YYYYMMDD}{id:06d}`，如 `CASE20260708000001`。
- 图片校验：jpg/png/webp，单文件 ≤ 10MB，MIME 用 magic number 校验。
- 密码哈希用 `bcrypt` 直接调用（不用 passlib，避免 bcrypt 4.x 兼容问题）。
- 核心逻辑走 TDD：先写失败测试 → 跑红 → 最小实现 → 跑绿 → 提交。
- 每个 Task 结束提交一次，提交信息用 `feat:`/`chore:`/`test:` 等常规前缀。

---

## File Structure

```text
backend/
├── pyproject.toml              # uv 项目 + 依赖
├── .env.example                # 环境变量模板
├── .gitignore
├── alembic.ini                 # Alembic 配置
├── alembic/
│   ├── env.py                  # 从 app.core.config 读 DATABASE_URL
│   ├── script.py.mako
│   └── versions/               # 迁移脚本
├── app/
│   ├── __init__.py
│   ├── main.py                 # FastAPI app + /health
│   ├── core/
│   │   ├── __init__.py
│   │   ├── config.py           # Settings (pydantic-settings)
│   │   ├── db.py               # engine, SessionLocal, get_db, Base
│   │   ├── security.py         # bcrypt 哈希 + JWT
│   │   └── deps.py             # get_current_user, require_role, get_camera_device
│   ├── models/
│   │   ├── __init__.py         # 导出所有模型（供 Alembic import）
│   │   ├── base.py             # Base 声明基类
│   │   ├── user.py             # Role, User
│   │   └── intake.py           # IntakeEvent, MediaAsset, Case, CameraDevice, CameraApiKey
│   ├── schemas/
│   │   ├── __init__.py
│   │   ├── auth.py             # LoginRequest, TokenResponse, UserOut, MenusOut
│   │   └── intake.py           # IntakeResponse 等
│   ├── services/
│   │   ├── __init__.py
│   │   ├── image_validate.py   # 格式/大小/MIME 校验
│   │   ├── image_hash.py       # sha256 去重
│   │   ├── storage.py          # 本地文件存储
│   │   └── intake_service.py   # 接入核心逻辑（建事件/案件/媒体/去重）
│   └── api/
│       ├── __init__.py
│       └── v1/
│           ├── __init__.py
│           ├── router.py       # 聚合 v1 路由
│           ├── auth.py         # /auth/login, /auth/me, /permissions/menus
│           └── intakes.py      # /intakes/citizen-reports, /camera-captures, /admin-uploads
└── tests/
    ├── __init__.py
    ├── conftest.py             # db/client/角色/用户/token fixtures
    ├── core/
    │   ├── __init__.py
    │   ├── test_security.py
    │   └── test_deps.py
    ├── api/
    │   ├── __init__.py
    │   ├── test_health.py
    │   ├── test_auth.py
    │   └── test_intakes.py
    └── services/
        ├── __init__.py
        ├── test_image_validate.py
        ├── test_image_hash.py
        ├── test_storage.py
        └── test_intake_service.py
```

**职责边界**：`core/` = 横切关注点（配置/DB/安全/依赖）；`models/` = ORM；`schemas/` = Pydantic DTO；`services/` = 业务逻辑（可独立单测）；`api/` = 薄路由，调 services。AI 适配器（Plan 2）将放 `app/ai/`。

---

## M0：项目骨架与环境

### Task 1: uv 项目 + 配置

**Files:**
- Create: `backend/pyproject.toml`
- Create: `backend/.env.example`
- Create: `backend/.gitignore`
- Create: `backend/app/__init__.py` (空)
- Create: `backend/app/core/__init__.py` (空)
- Create: `backend/app/core/config.py`
- Test: `backend/tests/__init__.py` (空), `backend/tests/core/__init__.py` (空)

**Interfaces:**
- Produces: `Settings` 类（字段见下），单例 `settings = Settings()`

- [ ] **Step 1: 创建 pyproject.toml**

```toml
[project]
name = "traffic-violation-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "sqlalchemy>=2.0",
    "alembic>=1.13",
    "pydantic>=2.6",
    "pydantic-settings>=2.1",
    "python-jose[cryptography]>=3.3",
    "bcrypt>=4.1,<5",
    "pymysql>=1.1",
    "python-multipart>=0.0.9",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "httpx>=0.27"]

[tool.pytest.ini_options]
testpaths = ["tests"]
```

- [ ] **Step 2: 创建 .env.example**

```env
DATABASE_URL=mysql+pymysql://root:root@localhost:3306/traffic_violation?charset=utf8mb4
REDIS_URL=redis://localhost:6379/0
JWT_SECRET=change-me-in-prod
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60
MEDIA_STORAGE_DIR=./media
MAX_IMAGE_SIZE=10485760
```

- [ ] **Step 3: 创建 .gitignore**

```gitignore
__pycache__/
*.pyc
.env
.venv/
media/
*.db
.pytest_cache/
```

- [ ] **Step 4: 创建 app/core/config.py**

```python
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", extra="ignore")

    DATABASE_URL: str = "mysql+pymysql://root:root@localhost:3306/traffic_violation?charset=utf8mb4"
    REDIS_URL: str = "redis://localhost:6379/0"
    JWT_SECRET: str = "change-me-in-prod"
    JWT_ALGORITHM: str = "HS256"
    JWT_EXPIRE_MINUTES: int = 60
    MEDIA_STORAGE_DIR: str = "./media"
    MAX_IMAGE_SIZE: int = 10 * 1024 * 1024
    ALLOWED_IMAGE_TYPES: tuple[str, ...] = ("image/jpeg", "image/png", "image/webp")


settings = Settings()
```

- [ ] **Step 5: 安装依赖并提交**

```bash
cd backend
uv sync --extra dev
```

```bash
git add backend/pyproject.toml backend/.env.example backend/.gitignore backend/app/__init__.py backend/app/core/__init__.py backend/app/core/config.py tests/__init__.py tests/core/__init__.py
git commit -m "chore: 初始化 uv 项目与配置"
```

---

### Task 2: FastAPI app + /health

**Files:**
- Create: `backend/app/main.py`
- Test: `backend/tests/api/__init__.py` (空), `backend/tests/api/test_health.py`
- Modify: `backend/tests/conftest.py`（创建，含 db/client 基础 fixture）

**Interfaces:**
- Produces: `app.main.app`（FastAPI 实例），`/health` 端点返回 `{"status":"ok"}`
- Produces (conftest): `client` fixture 返回 `TestClient`，`db` fixture 返回 `Session`（SQLite in-memory）

- [ ] **Step 1: 创建 conftest.py 基础 fixture**

```python
# tests/conftest.py
import pytest
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import Session, sessionmaker

from app.core.config import settings
from app.core.db import Base, get_db
from app.main import app

TEST_DB_URL = "sqlite:///:memory:"
test_engine = create_engine(TEST_DB_URL, connect_args={"check_same_thread": False})
TestSessionLocal = sessionmaker(bind=test_engine, autoflush=False, autocommit=False)


@pytest.fixture()
def db() -> Session:
    Base.metadata.create_all(bind=test_engine)
    session = TestSessionLocal()
    try:
        yield session
    finally:
        session.close()
        Base.metadata.drop_all(bind=test_engine)


@pytest.fixture()
def client(db: Session) -> TestClient:
    def _override_get_db():
        try:
            yield db
        finally:
            pass

    app.dependency_overrides[get_db] = _override_get_db
    yield TestClient(app)
    app.dependency_overrides.clear()
```

- [ ] **Step 2: 写失败测试 test_health.py**

```python
# tests/api/test_health.py
def test_health_returns_ok(client):
    response = client.get("/health")
    assert response.status_code == 200
    assert response.json() == {"status": "ok"}
```

- [ ] **Step 3: 跑测试确认失败**

Run: `cd backend && uv run pytest tests/api/test_health.py -v`
Expected: FAIL，`app.main` 模块不存在 / ImportError

- [ ] **Step 4: 创建 app/core/db.py（conftest 依赖它）**

```python
# app/core/db.py
from collections.abc import Generator

from sqlalchemy import create_engine
from sqlalchemy.orm import DeclarativeBase, Session, sessionmaker

from app.core.config import settings

engine = create_engine(settings.DATABASE_URL, pool_pre_ping=True)
SessionLocal = sessionmaker(bind=engine, autoflush=False, autocommit=False)


class Base(DeclarativeBase):
    pass


def get_db() -> Generator[Session, None, None]:
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

- [ ] **Step 5: 创建 app/main.py**

```python
# app/main.py
from fastapi import FastAPI

app = FastAPI(title="交通违章智能管理平台 API")


@app.get("/health")
def health() -> dict[str, str]:
    return {"status": "ok"}
```

- [ ] **Step 6: 跑测试确认通过**

Run: `cd backend && uv run pytest tests/api/test_health.py -v`
Expected: PASS

- [ ] **Step 7: 提交**

```bash
git add backend/app/core/db.py backend/app/main.py backend/tests/conftest.py backend/tests/api/__init__.py backend/tests/api/test_health.py
git commit -m "feat: FastAPI 骨架与 /health"
```

---

### Task 3: DB 连通性测试

**Files:**
- Test: `backend/tests/core/test_db.py`

**Interfaces:**
- Consumes: `Base`, `engine` from `app.core.db`

- [ ] **Step 1: 写测试（建一张临时表验证 Base.metadata 可建表）**

```python
# tests/core/test_db.py
from sqlalchemy import Column, Integer, String

from app.core.db import Base


class _Demo(Base):
    __tablename__ = "_demo"
    id = Column(Integer, primary_key=True)
    name = Column(String(32))


def test_base_can_create_and_drop(db):
    _Demo.__table__.create(db.get_bind(), checkfirst=True)
    db.add(_Demo(name="x"))
    db.commit()
    row = db.query(_Demo).first()
    assert row.name == "x"
```

- [ ] **Step 2: 跑测试**

Run: `cd backend && uv run pytest tests/core/test_db.py -v`
Expected: PASS（SQLite in-memory 建表+读写正常）

- [ ] **Step 3: 提交**

```bash
git add backend/tests/core/test_db.py
git commit -m "test: 验证 DB Base 可建表读写"
```

---

### Task 4: Alembic 初始化

**Files:**
- Create: `backend/alembic.ini`
- Create: `backend/alembic/env.py`
- Create: `backend/alembic/script.py.mako`
- Create: `backend/alembic/versions/.gitkeep`

**Interfaces:**
- Produces: Alembic 可用，`alembic upgrade head` 跑通（空 baseline 迁移）

- [ ] **Step 1: 初始化 Alembic**

```bash
cd backend
uv run alembic init alembic
```
（生成 `alembic.ini`、`alembic/env.py`、`alembic/script.py.mako`、`alembic/versions/`）

- [ ] **Step 2: 改 alembic.ini 的 sqlalchemy.url 为空（由 env.py 注入）**

将 `alembic.ini` 中 `sqlalchemy.url = ...` 一行改为：
```ini
sqlalchemy.url =
```

- [ ] **Step 3: 改 alembic/env.py，从 settings 读 URL 并导入 Base + 模型**

替换 `alembic/env.py` 全文为：

```python
# alembic/env.py
from logging.config import fileConfig

from alembic import context
from sqlalchemy import engine_from_config, pool

from app.core.config import settings
from app.core.db import Base
import app.models  # noqa: F401  确保模型被导入

config = context.config
config.set_main_option("sqlalchemy.url", settings.DATABASE_URL)

if config.config_file_name is not None:
    fileConfig(config.config_file_name)

target_metadata = Base.metadata


def run_migrations_offline() -> None:
    context.configure(url=settings.DATABASE_URL, target_metadata=target_metadata, literal_binds=True)
    with context.begin_transaction():
        context.run_migrations()


def run_migrations_online() -> None:
    connectable = engine_from_config(
        config.get_section(config.config_ini_section, {}),
        prefix="sqlalchemy.",
        poolclass=pool.NullPool,
    )
    with connectable.connect() as connection:
        context.configure(connection=connection, target_metadata=target_metadata)
        with context.begin_transaction():
            context.run_migrations()


if context.is_offline_mode():
    run_migrations_offline()
else:
    run_migrations_online()
```

- [ ] **Step 4: 创建空 baseline 迁移**

```bash
cd backend
uv run alembic revision -m "baseline"
```

- [ ] **Step 5: 在本地 MySQL 建库并跑迁移**

先在本地 MySQL 建空库：
```bash
mysql -uroot -p -e "CREATE DATABASE IF NOT EXISTS traffic_violation DEFAULT CHARSET utf8mb4;"
```
再跑迁移：
```bash
cd backend
uv run alembic upgrade head
```
Expected: `Running upgrade -> <rev>, baseline`（无报错）

- [ ] **Step 6: 提交**

```bash
git add backend/alembic.ini backend/alembic/
git commit -m "chore: 初始化 Alembic 与 baseline 迁移"
```

---

## M1：鉴权与 RBAC

### Task 5: Role + User 模型

**Files:**
- Create: `backend/app/models/__init__.py`
- Create: `backend/app/models/base.py`（如已有 Base 在 db.py，这里仅 re-export）
- Create: `backend/app/models/user.py`

**Interfaces:**
- Produces: `Role`（id, code, name, created_at）、`User`（id, username, password_hash, phone, email, role_id, role relationship, status, created_at）

- [ ] **Step 1: 写模型测试**

```python
# tests/core/test_models_user.py
from app.models.user import Role, User


def test_create_role_and_user(db):
    role = Role(code="citizen", name="市民")
    db.add(role)
    db.commit()
    user = User(username="u1", password_hash="h", email="u@e.com", role_id=role.id)
    db.add(user)
    db.commit()
    assert user.id is not None
    assert user.role.code == "citizen"
    assert user.status == "active"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd backend && uv run pytest tests/core/test_models_user.py -v`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 创建 app/models/base.py**

```python
# app/models/base.py
from app.core.db import Base  # 统一从 db.py 导出 Base

__all__ = ["Base"]
```

- [ ] **Step 4: 创建 app/models/user.py**

```python
# app/models/user.py
from datetime import datetime

from sqlalchemy import ForeignKey, String
from sqlalchemy.orm import Mapped, mapped_column, relationship

from app.models.base import Base


class Role(Base):
    __tablename__ = "roles"

    id: Mapped[int] = mapped_column(primary_key=True)
    code: Mapped[str] = mapped_column(String(32), unique=True, index=True)
    name: Mapped[str] = mapped_column(String(64))
    created_at: Mapped[datetime] = mapped_column(default=datetime.utcnow)


class User(Base):
    __tablename__ = "users"

    id: Mapped[int] = mapped_column(primary_key=True)
    username: Mapped[str] = mapped_column(String(64), unique=True, index=True)
    password_hash: Mapped[str] = mapped_column(String(255))
    phone: Mapped[str | None] = mapped_column(String(20))
    email: Mapped[str | None] = mapped_column(String(255))
    role_id: Mapped[int] = mapped_column(ForeignKey("roles.id"))
    role: Mapped["Role"] = relationship()
    status: Mapped[str] = mapped_column(String(20), default="active")
    created_at: Mapped[datetime] = mapped_column(default=datetime.utcnow)
```

- [ ] **Step 5: 创建 app/models/__init__.py（供 Alembic import）**

```python
# app/models/__init__.py
from app.models.base import Base
from app.models.user import Role, User

__all__ = ["Base", "Role", "User"]
```

- [ ] **Step 6: 跑测试确认通过**

Run: `cd backend && uv run pytest tests/core/test_models_user.py -v`
Expected: PASS

- [ ] **Step 7: 生成并应用迁移**

```bash
cd backend
uv run alembic revision --autogenerate -m "add roles and users"
uv run alembic upgrade head
```

- [ ] **Step 8: 提交**

```bash
git add backend/app/models/ backend/tests/core/test_models_user.py backend/alembic/versions/
git commit -m "feat: Role 与 User 模型"
```

---

### Task 6: 密码哈希（bcrypt）

**Files:**
- Create: `backend/app/core/security.py`
- Test: `backend/tests/core/test_security.py`

**Interfaces:**
- Produces: `hash_password(plain: str) -> str`、`verify_password(plain: str, hashed: str) -> bool`

- [ ] **Step 1: 写失败测试**

```python
# tests/core/test_security.py
from app.core.security import hash_password, verify_password


def test_hash_and_verify_password():
    hashed = hash_password("secret123")
    assert hashed != "secret123"
    assert verify_password("secret123", hashed) is True
    assert verify_password("wrong", hashed) is False
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd backend && uv run pytest tests/core/test_security.py -v`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 创建 app/core/security.py（哈希部分）**

```python
# app/core/security.py
import bcrypt


def hash_password(plain: str) -> str:
    return bcrypt.hashpw(plain.encode("utf-8"), bcrypt.gensalt()).decode("utf-8")


def verify_password(plain: str, hashed: str) -> bool:
    try:
        return bcrypt.checkpw(plain.encode("utf-8"), hashed.encode("utf-8"))
    except ValueError:
        return False
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd backend && uv run pytest tests/core/test_security.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/app/core/security.py backend/tests/core/test_security.py
git commit -m "feat: bcrypt 密码哈希"
```

---

### Task 7: JWT 令牌

**Files:**
- Modify: `backend/app/core/security.py`（追加 JWT 函数）
- Modify: `backend/tests/core/test_security.py`（追加 JWT 测试）

**Interfaces:**
- Produces: `create_access_token(subject: str, role: str, expires_minutes: int | None = None) -> str`、`decode_access_token(token: str) -> dict`（无效抛 `HTTPException(401)`）

- [ ] **Step 1: 追加失败测试**

在 `tests/core/test_security.py` 末尾追加：

```python
from fastapi import HTTPException

from app.core.security import create_access_token, decode_access_token


def test_create_and_decode_token():
    token = create_access_token(subject="42", role="citizen")
    payload = decode_access_token(token)
    assert payload["sub"] == "42"
    assert payload["role"] == "citizen"


def test_decode_invalid_token_raises():
    import pytest

    with pytest.raises(HTTPException) as exc:
        decode_access_token("not-a-token")
    assert exc.value.status_code == 401
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd backend && uv run pytest tests/core/test_security.py -v`
Expected: FAIL（`create_access_token` 未定义）

- [ ] **Step 3: 在 security.py 追加 JWT 实现**

在 `app/core/security.py` 顶部 import 区追加，并在文件末尾追加函数：

```python
# 顶部 import 区追加
from datetime import datetime, timedelta, timezone

from fastapi import HTTPException
from jose import JWTError, jwt

from app.core.config import settings
```

```python
# 文件末尾追加


def create_access_token(subject: str, role: str, expires_minutes: int | None = None) -> str:
    now = datetime.now(timezone.utc)
    exp = now + timedelta(minutes=expires_minutes if expires_minutes is not None else settings.JWT_EXPIRE_MINUTES)
    payload = {"sub": subject, "role": role, "exp": exp, "iat": now}
    return jwt.encode(payload, settings.JWT_SECRET, algorithm=settings.JWT_ALGORITHM)


def decode_access_token(token: str) -> dict:
    try:
        return jwt.decode(token, settings.JWT_SECRET, algorithms=[settings.JWT_ALGORITHM])
    except JWTError:
        raise HTTPException(status_code=401, detail="无效的令牌")
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd backend && uv run pytest tests/core/test_security.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/app/core/security.py backend/tests/core/test_security.py
git commit -m "feat: JWT 令牌签发与校验"
```

---

### Task 8: Auth schemas

**Files:**
- Create: `backend/app/schemas/__init__.py`（空）
- Create: `backend/app/schemas/auth.py`

**Interfaces:**
- Produces: `LoginRequest(username: str, password: str)`、`UserOut(id, username, role_code)`、`TokenResponse(access_token, token_type, user: UserOut)`、`MenusOut(menus: list[str])`

- [ ] **Step 1: 创建 app/schemas/auth.py**

```python
# app/schemas/auth.py
from pydantic import BaseModel


class LoginRequest(BaseModel):
    username: str
    password: str


class UserOut(BaseModel):
    id: int
    username: str
    role_code: str

    model_config = {"from_attributes": True}


class TokenResponse(BaseModel):
    access_token: str
    token_type: str = "bearer"
    user: UserOut


class MenusOut(BaseModel):
    menus: list[str]
```

- [ ] **Step 2: 提交**

```bash
git add backend/app/schemas/
git commit -m "feat: auth Pydantic schemas"
```

---

### Task 9: RBAC 依赖

**Files:**
- Create: `backend/app/core/deps.py`
- Modify: `backend/tests/conftest.py`（追加角色/用户/token fixtures）
- Test: `backend/tests/core/test_deps.py`

**Interfaces:**
- Consumes: `get_db` from `app.core.db`、`decode_access_token`、`User`/`Role` 模型
- Produces: `oauth2_scheme`、`get_current_user(token, db) -> User`、`require_role(*roles) -> callable` 依赖工厂

- [ ] **Step 1: 在 conftest.py 追加 fixtures**

在 `tests/conftest.py` 末尾追加：

```python
from app.core.security import create_access_token, hash_password
from app.models.user import Role, User


@pytest.fixture()
def seeded_roles(db: Session) -> dict[str, Role]:
    mapping = {
        "citizen": "市民",
        "reviewer": "审核员",
        "admin": "管理员",
        "camera": "摄像头",
    }
    result = {}
    for code, name in mapping.items():
        role = Role(code=code, name=name)
        db.add(role)
        result[code] = role
    db.commit()
    return result


@pytest.fixture()
def citizen_user(db: Session, seeded_roles) -> User:
    user = User(
        username="citizen1",
        password_hash=hash_password("pass1234"),
        email="citizen@example.com",
        role_id=seeded_roles["citizen"].id,
    )
    db.add(user)
    db.commit()
    return user


@pytest.fixture()
def citizen_token(citizen_user: User) -> str:
    return create_access_token(subject=str(citizen_user.id), role="citizen")


@pytest.fixture()
def auth_headers(citizen_token: str) -> dict[str, str]:
    return {"Authorization": f"Bearer {citizen_token}"}
```

- [ ] **Step 2: 写失败测试**

```python
# tests/core/test_deps.py
from fastapi import HTTPException

from app.core.deps import get_current_user, require_role


def test_get_current_user_returns_user(db, citizen_user, citizen_token):
    user = get_current_user(token=citizen_token, db=db)
    assert user.id == citizen_user.id


def test_get_current_user_invalid_token_raises(db):
    import pytest

    with pytest.raises(HTTPException) as exc:
        get_current_user(token="bad", db=db)
    assert exc.value.status_code == 401


def test_require_role_allows(db, citizen_user, citizen_token):
    checker = require_role("citizen", "admin")
    user = checker(user=citizen_user)
    assert user.id == citizen_user.id


def test_require_role_denies(db, citizen_user):
    import pytest

    checker = require_role("admin")
    with pytest.raises(HTTPException) as exc:
        checker(user=citizen_user)
    assert exc.value.status_code == 403
```

- [ ] **Step 3: 跑测试确认失败**

Run: `cd backend && uv run pytest tests/core/test_deps.py -v`
Expected: FAIL（`app.core.deps` 不存在）

- [ ] **Step 4: 创建 app/core/deps.py**

```python
# app/core/deps.py
from fastapi import Depends, HTTPException, status
from fastapi.security import OAuth2PasswordBearer
from sqlalchemy.orm import Session

from app.core.db import get_db
from app.core.security import decode_access_token
from app.models.user import User

oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/api/v1/auth/login")


def get_current_user(token: str = Depends(oauth2_scheme), db: Session = Depends(get_db)) -> User:
    payload = decode_access_token(token)
    try:
        user_id = int(payload["sub"])
    except (KeyError, ValueError):
        raise HTTPException(status_code=401, detail="无效的令牌")
    user = db.get(User, user_id)
    if user is None or user.status != "active":
        raise HTTPException(status_code=401, detail="用户不存在或已禁用")
    return user


def require_role(*roles: str):
    def checker(user: User = Depends(get_current_user)) -> User:
        if user.role.code not in roles:
            raise HTTPException(status_code=403, detail="权限不足")
        return user

    return checker
```

- [ ] **Step 5: 跑测试确认通过**

Run: `cd backend && uv run pytest tests/core/test_deps.py -v`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add backend/app/core/deps.py backend/tests/conftest.py backend/tests/core/test_deps.py
git commit -m "feat: RBAC 鉴权依赖"
```

---

### Task 10: /auth/login

**Files:**
- Create: `backend/app/api/__init__.py`（空）
- Create: `backend/app/api/v1/__init__.py`（空）
- Create: `backend/app/api/v1/auth.py`
- Create: `backend/app/api/v1/router.py`
- Modify: `backend/app/main.py`（挂载 v1 路由）
- Test: `backend/tests/api/test_auth.py`

**Interfaces:**
- Produces: `POST /api/v1/auth/login` 接收 `LoginRequest`，校验密码，返回 `TokenResponse`

- [ ] **Step 1: 写失败测试**

```python
# tests/api/test_auth.py
def test_login_success(client, citizen_user):
    response = client.post(
        "/api/v1/auth/login",
        json={"username": "citizen1", "password": "pass1234"},
    )
    assert response.status_code == 200
    data = response.json()
    assert data["token_type"] == "bearer"
    assert data["access_token"]
    assert data["user"]["username"] == "citizen1"
    assert data["user"]["role_code"] == "citizen"


def test_login_wrong_password(client, citizen_user):
    response = client.post(
        "/api/v1/auth/login",
        json={"username": "citizen1", "password": "wrong"},
    )
    assert response.status_code == 401
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd backend && uv run pytest tests/api/test_auth.py -v`
Expected: FAIL（路由不存在，404）

- [ ] **Step 3: 创建 app/api/v1/auth.py**

```python
# app/api/v1/auth.py
from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.orm import Session

from app.core.db import get_db
from app.core.security import create_access_token, verify_password
from app.models.user import User
from app.schemas.auth import LoginRequest, TokenResponse, UserOut

router = APIRouter(prefix="/auth", tags=["auth"])


@router.post("/login", response_model=TokenResponse)
def login(body: LoginRequest, db: Session = Depends(get_db)) -> TokenResponse:
    user = db.query(User).filter(User.username == body.username).first()
    if user is None or not verify_password(body.password, user.password_hash):
        raise HTTPException(status_code=401, detail="用户名或密码错误")
    if user.status != "active":
        raise HTTPException(status_code=403, detail="用户已禁用")
    token = create_access_token(subject=str(user.id), role=user.role.code)
    return TokenResponse(
        access_token=token,
        user=UserOut(id=user.id, username=user.username, role_code=user.role.code),
    )
```

- [ ] **Step 4: 创建 app/api/v1/router.py**

```python
# app/api/v1/router.py
from fastapi import APIRouter

from app.api.v1 import auth

api_router = APIRouter(prefix="/api/v1")
api_router.include_router(auth.router)
```

- [ ] **Step 5: 修改 app/main.py 挂载路由**

```python
# app/main.py
from fastapi import FastAPI

from app.api.v1.router import api_router

app = FastAPI(title="交通违章智能管理平台 API")
app.include_router(api_router)


@app.get("/health")
def health() -> dict[str, str]:
    return {"status": "ok"}
```

- [ ] **Step 6: 跑测试确认通过**

Run: `cd backend && uv run pytest tests/api/test_auth.py -v`
Expected: PASS

- [ ] **Step 7: 提交**

```bash
git add backend/app/api/ backend/app/main.py backend/tests/api/test_auth.py
git commit -m "feat: /auth/login 登录接口"
```

---

### Task 11: /auth/me + /permissions/menus

**Files:**
- Modify: `backend/app/api/v1/auth.py`
- Modify: `backend/tests/api/test_auth.py`

**Interfaces:**
- Consumes: `get_current_user`、`require_role`
- Produces: `GET /api/v1/auth/me` 返回 `UserOut`；`GET /api/v1/permissions/menus` 返回 `MenusOut`（静态角色→菜单映射）

- [ ] **Step 1: 追加失败测试**

在 `tests/api/test_auth.py` 末尾追加：

```python
def test_me_requires_auth(client):
    response = client.get("/api/v1/auth/me")
    assert response.status_code == 401


def test_me_returns_user(client, citizen_user, auth_headers):
    response = client.get("/api/v1/auth/me", headers=auth_headers)
    assert response.status_code == 200
    assert response.json()["username"] == "citizen1"


def test_menus_returns_role_menus(client, citizen_user, auth_headers):
    response = client.get("/api/v1/permissions/menus", headers=auth_headers)
    assert response.status_code == 200
    assert "citizen_report" in response.json()["menus"]
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd backend && uv run pytest tests/api/test_auth.py -v`
Expected: FAIL（`/auth/me` 不存在）

- [ ] **Step 3: 在 auth.py 追加 /auth/me 与 permissions/menus**

在 `app/api/v1/auth.py` 顶部 import 区追加：

```python
from app.core.deps import get_current_user
from app.schemas.auth import MenusOut
```

在文件末尾追加（`/auth/me` 挂在 `router` 上；`/permissions/menus` 用单独的 `permissions_router`，因为前缀不同）：

```python
ROLE_MENUS: dict[str, list[str]] = {
    "citizen": ["citizen_report", "my_violations"],
    "reviewer": ["review_workbench", "violations_query"],
    "admin": ["review_workbench", "violations_query", "system_management", "statistics"],
    "camera": [],
}


@router.get("/me", response_model=UserOut)
def me(user: User = Depends(get_current_user)) -> UserOut:
    return UserOut(id=user.id, username=user.username, role_code=user.role.code)


permissions_router = APIRouter(prefix="/permissions", tags=["permissions"])


@permissions_router.get("/menus", response_model=MenusOut)
def menus(user: User = Depends(get_current_user)) -> MenusOut:
    return MenusOut(menus=ROLE_MENUS.get(user.role.code, []))
```

在 `app/api/v1/router.py` 追加挂载 `permissions_router`：

```python
from app.api.v1.auth import permissions_router

api_router.include_router(permissions_router)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd backend && uv run pytest tests/api/test_auth.py -v`
Expected: PASS（3 个新测试通过，原 2 个不回归）

- [ ] **Step 5: 提交**

```bash
git add backend/app/api/v1/auth.py backend/app/api/v1/router.py backend/tests/api/test_auth.py
git commit -m "feat: /auth/me 与 /permissions/menus"
```

---

## M2：图片接入与存储

### Task 12: Intake / Case / Camera 模型

**Files:**
- Create: `backend/app/models/intake.py`
- Modify: `backend/app/models/__init__.py`（导出新模型）

**Interfaces:**
- Produces: `IntakeEvent`、`MediaAsset`、`Case`、`CameraDevice`、`CameraApiKey`

- [ ] **Step 1: 写模型测试**

```python
# tests/core/test_models_intake.py
from app.models.intake import CameraApiKey, CameraDevice, Case, IntakeEvent, MediaAsset


def test_create_intake_and_case(db):
    event = IntakeEvent(
        source_type="citizen", source_id=1, location_text="路口A", image_hash="abc",
    )
    db.add(event)
    db.commit()
    case = Case(case_no="CASE20260708000001", intake_event_id=event.id, status="uploaded")
    db.add(case)
    db.commit()
    media = MediaAsset(intake_event_id=event.id, asset_type="original", url="/media/x.jpg", mime_type="image/jpeg", size=1024)
    db.add(media)
    db.commit()
    assert case.id is not None
    assert case.intake_event.image_hash == "abc"


def test_create_camera_and_key(db):
    dev = CameraDevice(device_code="CAM01", location_text="路口B")
    db.add(dev)
    db.commit()
    key = CameraApiKey(camera_device_id=dev.id, key_hash="hashed")
    db.add(key)
    db.commit()
    assert key.id is not None
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd backend && uv run pytest tests/core/test_models_intake.py -v`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 创建 app/models/intake.py**

```python
# app/models/intake.py
from datetime import datetime

from sqlalchemy import Float, ForeignKey, String
from sqlalchemy.orm import Mapped, mapped_column, relationship

from app.models.base import Base


class IntakeEvent(Base):
    __tablename__ = "intake_events"

    id: Mapped[int] = mapped_column(primary_key=True)
    source_type: Mapped[str] = mapped_column(String(16))  # citizen/camera/admin
    source_id: Mapped[int | None] = mapped_column()  # 举报人/管理员/摄像头 id
    location_text: Mapped[str | None] = mapped_column(String(255))
    longitude: Mapped[float | None] = mapped_column(Float)
    latitude: Mapped[float | None] = mapped_column(Float)
    captured_at: Mapped[datetime | None] = mapped_column()
    speed: Mapped[float | None] = mapped_column(Float)
    image_hash: Mapped[str] = mapped_column(String(64), index=True)
    status: Mapped[str] = mapped_column(String(16), default="uploaded")
    created_at: Mapped[datetime] = mapped_column(default=datetime.utcnow)

    cases: Mapped[list["Case"]] = relationship(back_populates="intake_event")


class MediaAsset(Base):
    __tablename__ = "media_assets"

    id: Mapped[int] = mapped_column(primary_key=True)
    intake_event_id: Mapped[int] = mapped_column(ForeignKey("intake_events.id"))
    asset_type: Mapped[str] = mapped_column(String(16))  # original/annotated/cropped_plate
    url: Mapped[str] = mapped_column(String(512))
    mime_type: Mapped[str] = mapped_column(String(64))
    size: Mapped[int] = mapped_column()
    created_at: Mapped[datetime] = mapped_column(default=datetime.utcnow)


class Case(Base):
    __tablename__ = "cases"

    id: Mapped[int] = mapped_column(primary_key=True)
    case_no: Mapped[str] = mapped_column(String(32), unique=True, index=True)
    intake_event_id: Mapped[int] = mapped_column(ForeignKey("intake_events.id"))
    intake_event: Mapped["IntakeEvent"] = relationship(back_populates="cases")
    status: Mapped[str] = mapped_column(String(24), default="uploaded")
    plate_no: Mapped[str | None] = mapped_column(String(16))
    violation_type: Mapped[str | None] = mapped_column(String(32))
    reviewer_id: Mapped[int | None] = mapped_column()
    review_opinion: Mapped[str | None] = mapped_column(String(512))
    reviewed_at: Mapped[datetime | None] = mapped_column()
    created_at: Mapped[datetime] = mapped_column(default=datetime.utcnow)


class CameraDevice(Base):
    __tablename__ = "camera_devices"

    id: Mapped[int] = mapped_column(primary_key=True)
    device_code: Mapped[str] = mapped_column(String(32), unique=True, index=True)
    location_text: Mapped[str | None] = mapped_column(String(255))
    status: Mapped[str] = mapped_column(String(16), default="enabled")
    created_at: Mapped[datetime] = mapped_column(default=datetime.utcnow)


class CameraApiKey(Base):
    __tablename__ = "camera_api_keys"

    id: Mapped[int] = mapped_column(primary_key=True)
    camera_device_id: Mapped[int] = mapped_column(ForeignKey("camera_devices.id"))
    key_hash: Mapped[str] = mapped_column(String(255), unique=True, index=True)
    status: Mapped[str] = mapped_column(String(16), default="active")
    created_at: Mapped[datetime] = mapped_column(default=datetime.utcnow)
```

- [ ] **Step 4: 更新 app/models/__init__.py**

```python
# app/models/__init__.py
from app.models.base import Base
from app.models.intake import CameraApiKey, CameraDevice, Case, IntakeEvent, MediaAsset
from app.models.user import Role, User

__all__ = [
    "Base", "Role", "User",
    "IntakeEvent", "MediaAsset", "Case", "CameraDevice", "CameraApiKey",
]
```

- [ ] **Step 5: 跑测试确认通过**

Run: `cd backend && uv run pytest tests/core/test_models_intake.py -v`
Expected: PASS

- [ ] **Step 6: 生成并应用迁移**

```bash
cd backend
uv run alembic revision --autogenerate -m "add intake, media, case, camera tables"
uv run alembic upgrade head
```

- [ ] **Step 7: 提交**

```bash
git add backend/app/models/ backend/tests/core/test_models_intake.py backend/alembic/versions/
git commit -m "feat: 接入/案件/摄像头模型"
```

---

### Task 13: 图片校验（格式/大小/MIME）

**Files:**
- Create: `backend/app/services/__init__.py`（空）
- Create: `backend/app/services/image_validate.py`
- Test: `backend/tests/services/__init__.py`（空）, `backend/tests/services/test_image_validate.py`

**Interfaces:**
- Produces: `validate_image(data: bytes, filename: str) -> str`，返回 MIME 类型；不合法抛 `HTTPException(400)`

- [ ] **Step 1: 写失败测试**

```python
# tests/services/test_image_validate.py
import pytest
from fastapi import HTTPException

from app.services.image_validate import validate_image

JPEG = b"\xff\xd8\xff\xe0" + b"\x00" * 100
PNG = b"\x89PNG\r\n\x1a\n" + b"\x00" * 100


def test_validate_jpeg():
    assert validate_image(JPEG, "a.jpg") == "image/jpeg"


def test_validate_png():
    assert validate_image(PNG, "a.png") == "image/png"


def test_validate_rejects_txt():
    with pytest.raises(HTTPException) as exc:
        validate_image(b"hello world", "a.txt")
    assert exc.value.status_code == 400


def test_validate_rejects_oversize():
    big = b"\xff\xd8\xff\xe0" + b"\x00" * (11 * 1024 * 1024)
    with pytest.raises(HTTPException) as exc:
        validate_image(big, "a.jpg")
    assert exc.value.status_code == 400
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd backend && uv run pytest tests/services/test_image_validate.py -v`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 创建 app/services/image_validate.py**

```python
# app/services/image_validate.py
from fastapi import HTTPException

from app.core.config import settings

_MAGIC = [
    (b"\xff\xd8\xff", "image/jpeg"),
    (b"\x89PNG\r\n\x1a\n", "image/png"),
    (b"RIFF", "image/webp"),  # 进一步校验 WEBP 八字
]


def _sniff_mime(data: bytes) -> str | None:
    for magic, mime in _MAGIC:
        if data.startswith(magic):
            if mime == "image/webp" and data[8:12] != b"WEBP":
                return None
            return mime
    return None


def validate_image(data: bytes, filename: str) -> str:
    if len(data) > settings.MAX_IMAGE_SIZE:
        raise HTTPException(status_code=400, detail="图片过大")
    mime = _sniff_mime(data)
    if mime is None or mime not in settings.ALLOWED_IMAGE_TYPES:
        raise HTTPException(status_code=400, detail="不支持的图片格式")
    return mime
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd backend && uv run pytest tests/services/test_image_validate.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/app/services/__init__.py backend/app/services/image_validate.py backend/tests/services/__init__.py backend/tests/services/test_image_validate.py
git commit -m "feat: 图片格式/大小/MIME 校验"
```

---

### Task 14: 图片哈希（去重）

**Files:**
- Create: `backend/app/services/image_hash.py`
- Test: `backend/tests/services/test_image_hash.py`

**Interfaces:**
- Produces: `compute_image_hash(data: bytes) -> str`（返回 sha256 hex）

- [ ] **Step 1: 写失败测试**

```python
# tests/services/test_image_hash.py
from app.services.image_hash import compute_image_hash


def test_hash_stable():
    assert compute_image_hash(b"abc") == compute_image_hash(b"abc")


def test_hash_differs():
    assert compute_image_hash(b"abc") != compute_image_hash(b"abd")


def test_hash_is_hex():
    h = compute_image_hash(b"abc")
    assert len(h) == 64
    int(h, 16)  # 不抛即合法十六进制
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd backend && uv run pytest tests/services/test_image_hash.py -v`
Expected: FAIL

- [ ] **Step 3: 创建 app/services/image_hash.py**

```python
# app/services/image_hash.py
import hashlib


def compute_image_hash(data: bytes) -> str:
    return hashlib.sha256(data).hexdigest()
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd backend && uv run pytest tests/services/test_image_hash.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/app/services/image_hash.py backend/tests/services/test_image_hash.py
git commit -m "feat: 图片 sha256 哈希"
```

---

### Task 15: 本地文件存储

**Files:**
- Create: `backend/app/services/storage.py`
- Test: `backend/tests/services/test_storage.py`

**Interfaces:**
- Produces: `save_media(data: bytes, mime: str) -> tuple[str, str]`，返回 `(relative_url, ext)`；文件写入 `MEDIA_STORAGE_DIR`

- [ ] **Step 1: 写失败测试**

```python
# tests/services/test_storage.py
import os

from app.services.storage import save_media


def test_save_media_writes_file(tmp_path, monkeypatch):
    monkeypatch.setattr("app.services.storage.settings.MEDIA_STORAGE_DIR", str(tmp_path))
    data = b"\xff\xd8\xff\xe0" + b"\x00" * 10
    url, ext = save_media(data, "image/jpeg")
    assert ext == "jpg"
    filename = url.split("/")[-1]
    path = os.path.join(str(tmp_path), filename)
    assert os.path.exists(path)
    with open(path, "rb") as f:
        assert f.read() == data
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd backend && uv run pytest tests/services/test_storage.py -v`
Expected: FAIL

- [ ] **Step 3: 创建 app/services/storage.py**

```python
# app/services/storage.py
import os
import uuid

from app.core.config import settings

_MIME_EXT = {"image/jpeg": "jpg", "image/png": "png", "image/webp": "webp"}


def save_media(data: bytes, mime: str) -> tuple[str, str]:
    os.makedirs(settings.MEDIA_STORAGE_DIR, exist_ok=True)
    ext = _MIME_EXT.get(mime, "bin")
    filename = f"{uuid.uuid4().hex}.{ext}"
    path = os.path.join(settings.MEDIA_STORAGE_DIR, filename)
    with open(path, "wb") as f:
        f.write(data)
    return f"/media/{filename}", ext
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd backend && uv run pytest tests/services/test_storage.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/app/services/storage.py backend/tests/services/test_storage.py
git commit -m "feat: 本地媒体文件存储"
```

---

### Task 16: Intake schemas

**Files:**
- Create: `backend/app/schemas/intake.py`

**Interfaces:**
- Produces: `IntakeResponse(case_id: int, case_no: str, status: str, message: str)`

- [ ] **Step 1: 创建 app/schemas/intake.py**

```python
# app/schemas/intake.py
from pydantic import BaseModel


class IntakeResponse(BaseModel):
    case_id: int
    case_no: str
    status: str
    message: str
```

- [ ] **Step 2: 提交**

```bash
git add backend/app/schemas/intake.py
git commit -m "feat: intake 响应 schema"
```

---

### Task 17: Intake service（核心接入逻辑）

**Files:**
- Create: `backend/app/services/intake_service.py`
- Test: `backend/tests/services/test_intake_service.py`
- Modify: `backend/tests/conftest.py`（追加 camera 设备/key fixture，见 Step 1）

**Interfaces:**
- Consumes: `validate_image`、`compute_image_hash`、`save_media`、模型
- Produces: `create_intake(db, *, source_type, source_id, image_bytes, filename, location_text, captured_at=None, speed=None, longitude=None, latitude=None) -> Case`；重复图片抛 `HTTPException(409)`

- [ ] **Step 1: 在 conftest.py 追加 camera fixture**

在 `tests/conftest.py` 末尾追加（`hash_password` 已在 Task 9 的 conftest 追加中导入，这里不重复 import）：

```python
from app.models.intake import CameraApiKey, CameraDevice


@pytest.fixture()
def camera_device(db: Session) -> CameraDevice:
    dev = CameraDevice(device_code="CAM01", location_text="路口B")
    db.add(dev)
    db.commit()
    return dev


@pytest.fixture()
def camera_key(db: Session, camera_device: CameraDevice) -> tuple[str, CameraApiKey]:
    raw = "cam-key-123"
    key = CameraApiKey(camera_device_id=camera_device.id, key_hash=hash_password(raw))
    db.add(key)
    db.commit()
    return raw, key
```

- [ ] **Step 2: 写失败测试**

```python
# tests/services/test_intake_service.py
import pytest
from fastapi import HTTPException

from app.services.intake_service import create_intake

JPEG = b"\xff\xd8\xff\xe0" + b"\x00" * 10


def test_create_intake_citizen(db, citizen_user, tmp_path, monkeypatch):
    monkeypatch.setattr("app.services.storage.settings.MEDIA_STORAGE_DIR", str(tmp_path))
    case = create_intake(
        db,
        source_type="citizen",
        source_id=citizen_user.id,
        image_bytes=JPEG,
        filename="a.jpg",
        location_text="路口A",
    )
    assert case.id is not None
    assert case.case_no.startswith("CASE")
    assert case.status == "uploaded"
    assert case.intake_event.image_hash
    assets = case.intake_event.media_assets
    assert len(assets) == 1
    assert assets[0].asset_type == "original"


def test_create_intake_duplicate_rejected(db, citizen_user, tmp_path, monkeypatch):
    monkeypatch.setattr("app.services.storage.settings.MEDIA_STORAGE_DIR", str(tmp_path))
    create_intake(
        db, source_type="citizen", source_id=citizen_user.id,
        image_bytes=JPEG, filename="a.jpg", location_text="路口A",
    )
    with pytest.raises(HTTPException) as exc:
        create_intake(
            db, source_type="citizen", source_id=citizen_user.id,
            image_bytes=JPEG, filename="a.jpg", location_text="路口A",
        )
    assert exc.value.status_code == 409
```

- [ ] **Step 3: 跑测试确认失败**

Run: `cd backend && uv run pytest tests/services/test_intake_service.py -v`
Expected: FAIL（模块不存在）

- [ ] **Step 4: 创建 app/services/intake_service.py**

```python
# app/services/intake_service.py
from datetime import datetime

from fastapi import HTTPException
from sqlalchemy.orm import Session

from app.models.intake import Case, IntakeEvent, MediaAsset
from app.services.image_hash import compute_image_hash
from app.services.image_validate import validate_image
from app.services.storage import save_media


def _gen_case_no(case_id: int) -> str:
    return f"CASE{datetime.utcnow():%Y%m%d}{case_id:06d}"


def create_intake(
    db: Session,
    *,
    source_type: str,
    source_id: int | None,
    image_bytes: bytes,
    filename: str,
    location_text: str | None,
    captured_at: datetime | None = None,
    speed: float | None = None,
    longitude: float | None = None,
    latitude: float | None = None,
) -> Case:
    mime = validate_image(image_bytes, filename)
    image_hash = compute_image_hash(image_bytes)

    existing = db.query(IntakeEvent).filter(IntakeEvent.image_hash == image_hash).first()
    if existing is not None:
        raise HTTPException(status_code=409, detail="重复举报，该图片已存在")

    url, _ext = save_media(image_bytes, mime)

    event = IntakeEvent(
        source_type=source_type,
        source_id=source_id,
        location_text=location_text,
        longitude=longitude,
        latitude=latitude,
        captured_at=captured_at,
        speed=speed,
        image_hash=image_hash,
        status="uploaded",
    )
    db.add(event)
    db.flush()  # 拿 event.id

    db.add(MediaAsset(
        intake_event_id=event.id,
        asset_type="original",
        url=url,
        mime_type=mime,
        size=len(image_bytes),
    ))

    case = Case(case_no="placeholder", intake_event_id=event.id, status="uploaded")
    db.add(case)
    db.flush()
    case.case_no = _gen_case_no(case.id)
    db.commit()
    db.refresh(case)
    return case
```

- [ ] **Step 5: 跑测试确认通过**

Run: `cd backend && uv run pytest tests/services/test_intake_service.py -v`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add backend/app/services/intake_service.py backend/tests/services/test_intake_service.py backend/tests/conftest.py
git commit -m "feat: 接入核心服务（建事件/案件/媒体/去重）"
```

---

### Task 18: /intakes/citizen-reports + /intakes/admin-uploads

**Files:**
- Create: `backend/app/api/v1/intakes.py`
- Modify: `backend/app/api/v1/router.py`
- Test: `backend/tests/api/test_intakes.py`

**Interfaces:**
- Consumes: `create_intake`、`require_role`、`get_current_user`
- Produces: `POST /api/v1/intakes/citizen-reports`（citizen 角色）、`POST /api/v1/intakes/admin-uploads`（reviewer/admin 角色），均 multipart 上传 `image` 字段

- [ ] **Step 1: 写失败测试**

```python
# tests/api/test_intakes.py
JPEG = b"\xff\xd8\xff\xe0" + b"\x00" * 10


def test_citizen_upload_success(client, citizen_user, auth_headers, tmp_path, monkeypatch):
    monkeypatch.setattr("app.services.storage.settings.MEDIA_STORAGE_DIR", str(tmp_path))
    response = client.post(
        "/api/v1/intakes/citizen-reports",
        headers=auth_headers,
        files={"image": ("a.jpg", JPEG, "image/jpeg")},
        data={"location_text": "路口A"},
    )
    assert response.status_code == 200
    data = response.json()
    assert data["case_no"].startswith("CASE")
    assert data["status"] == "uploaded"


def test_citizen_upload_requires_auth(client):
    response = client.post(
        "/api/v1/intakes/citizen-reports",
        files={"image": ("a.jpg", JPEG, "image/jpeg")},
        data={"location_text": "路口A"},
    )
    assert response.status_code == 401


def test_citizen_upload_duplicate(client, citizen_user, auth_headers, tmp_path, monkeypatch):
    monkeypatch.setattr("app.services.storage.settings.MEDIA_STORAGE_DIR", str(tmp_path))
    client.post(
        "/api/v1/intakes/citizen-reports",
        headers=auth_headers,
        files={"image": ("a.jpg", JPEG, "image/jpeg")},
        data={"location_text": "路口A"},
    )
    response = client.post(
        "/api/v1/intakes/citizen-reports",
        headers=auth_headers,
        files={"image": ("a.jpg", JPEG, "image/jpeg")},
        data={"location_text": "路口A"},
    )
    assert response.status_code == 409
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd backend && uv run pytest tests/api/test_intakes.py -v`
Expected: FAIL（路由不存在）

- [ ] **Step 3: 创建 app/api/v1/intakes.py**

```python
# app/api/v1/intakes.py
from fastapi import APIRouter, Depends, File, Form, UploadFile
from sqlalchemy.orm import Session

from app.core.db import get_db
from app.core.deps import require_role
from app.models.user import User
from app.schemas.intake import IntakeResponse
from app.services.intake_service import create_intake

router = APIRouter(prefix="/intakes", tags=["intakes"])


def _to_response(case) -> IntakeResponse:
    return IntakeResponse(case_id=case.id, case_no=case.case_no, status=case.status, message="图片已接收，等待处理")


@router.post("/citizen-reports", response_model=IntakeResponse)
def citizen_report(
    image: UploadFile = File(...),
    location_text: str = Form(None),
    db: Session = Depends(get_db),
    user: User = Depends(require_role("citizen")),
) -> IntakeResponse:
    data = image.file.read()
    case = create_intake(
        db, source_type="citizen", source_id=user.id,
        image_bytes=data, filename=image.filename or "upload.jpg",
        location_text=location_text,
    )
    return _to_response(case)


@router.post("/admin-uploads", response_model=IntakeResponse)
def admin_upload(
    image: UploadFile = File(...),
    location_text: str = Form(None),
    db: Session = Depends(get_db),
    user: User = Depends(require_role("reviewer", "admin")),
) -> IntakeResponse:
    data = image.file.read()
    case = create_intake(
        db, source_type="admin", source_id=user.id,
        image_bytes=data, filename=image.filename or "upload.jpg",
        location_text=location_text,
    )
    return _to_response(case)
```

- [ ] **Step 4: 在 router.py 挂载 intakes 路由**

在 `app/api/v1/router.py` 追加：

```python
from app.api.v1 import intakes

api_router.include_router(intakes.router)
```

- [ ] **Step 5: 跑测试确认通过**

Run: `cd backend && uv run pytest tests/api/test_intakes.py -v`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add backend/app/api/v1/intakes.py backend/app/api/v1/router.py backend/tests/api/test_intakes.py
git commit -m "feat: 市民/后台图片接入接口"
```

---

### Task 19: 摄像头 API Key 鉴权 + /intakes/camera-captures

**Files:**
- Modify: `backend/app/core/deps.py`（追加 `get_camera_device`）
- Modify: `backend/app/api/v1/intakes.py`（追加 camera-captures 端点）
- Modify: `backend/tests/api/test_intakes.py`（追加 camera 测试）

**Interfaces:**
- Consumes: `hash_password`/`verify_password`、`CameraApiKey`/`CameraDevice`
- Produces: `get_camera_device(x_camera_key: str = Header(...)) -> CameraDevice` 依赖；`POST /api/v1/intakes/camera-captures`（`X-Camera-Key` 头鉴权，可选 `speed` 字段）

- [ ] **Step 1: 追加失败测试**

在 `tests/api/test_intakes.py` 末尾追加：

```python
def test_camera_capture_success(client, camera_key, tmp_path, monkeypatch):
    raw_key, _ = camera_key
    monkeypatch.setattr("app.services.storage.settings.MEDIA_STORAGE_DIR", str(tmp_path))
    response = client.post(
        "/api/v1/intakes/camera-captures",
        headers={"X-Camera-Key": raw_key},
        files={"image": ("a.jpg", JPEG, "image/jpeg")},
        data={"location_text": "路口B", "speed": "88"},
    )
    assert response.status_code == 200
    data = response.json()
    assert data["case_no"].startswith("CASE")


def test_camera_capture_bad_key(client):
    response = client.post(
        "/api/v1/intakes/camera-captures",
        headers={"X-Camera-Key": "wrong"},
        files={"image": ("a.jpg", JPEG, "image/jpeg")},
        data={"location_text": "路口B"},
    )
    assert response.status_code == 401
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd backend && uv run pytest tests/api/test_intakes.py::test_camera_capture_success -v`
Expected: FAIL（路由不存在）

- [ ] **Step 3: 在 deps.py 追加 get_camera_device**

在 `app/core/deps.py` 顶部 import 区追加：

```python
from app.models.intake import CameraApiKey, CameraDevice
from app.core.security import verify_password
from fastapi import Header
```

在文件末尾追加：

```python
def get_camera_device(
    x_camera_key: str = Header(..., alias="X-Camera-Key"),
    db: Session = Depends(get_db),
) -> CameraDevice:
    keys = db.query(CameraApiKey).filter(CameraApiKey.status == "active").all()
    for k in keys:
        if verify_password(x_camera_key, k.key_hash):
            dev = db.get(CameraDevice, k.camera_device_id)
            if dev and dev.status == "enabled":
                return dev
    raise HTTPException(status_code=401, detail="无效的摄像头密钥")
```

- [ ] **Step 4: 在 intakes.py 追加 camera-captures 端点**

在 `app/api/v1/intakes.py` 顶部 import 区追加：

```python
from app.core.deps import get_camera_device
from app.models.intake import CameraDevice
```

在文件末尾追加：

```python
@router.post("/camera-captures", response_model=IntakeResponse)
def camera_capture(
    image: UploadFile = File(...),
    location_text: str = Form(None),
    speed: float | None = Form(None),
    db: Session = Depends(get_db),
    device: CameraDevice = Depends(get_camera_device),
) -> IntakeResponse:
    data = image.file.read()
    case = create_intake(
        db, source_type="camera", source_id=device.id,
        image_bytes=data, filename=image.filename or "capture.jpg",
        location_text=location_text or device.location_text,
        speed=speed,
    )
    return _to_response(case)
```

- [ ] **Step 5: 跑测试确认通过**

Run: `cd backend && uv run pytest tests/api/test_intakes.py -v`
Expected: PASS（全部用例通过，无回归）

- [ ] **Step 6: 跑全量测试**

Run: `cd backend && uv run pytest -v`
Expected: 全部 PASS

- [ ] **Step 7: 提交**

```bash
git add backend/app/core/deps.py backend/app/api/v1/intakes.py backend/tests/api/test_intakes.py
git commit -m "feat: 摄像头 API Key 鉴权与抓拍接入"
```

---

## 完成标准（Plan 1）

- `cd backend && uv run uvicorn app.main:app --reload` 启动无报错，`GET /health` 返回 200。
- `alembic upgrade head` 在本地 MySQL 跑通，`roles/users/intake_events/media_assets/cases/camera_devices/camera_api_keys` 表已建。
- 四个角色可登录拿 JWT；`/auth/me`、`/permissions/menus` 鉴权生效。
- 市民/后台/摄像头三种上传均能建 `intake_event` + `case`（status=uploaded）+ 存原图，返回 `case_no`。
- 重复图片上传返回 409。
- `uv run pytest` 全绿。

## 后续衔接

- Plan 2（M3–M5）：Celery + YOLOv8 检测 + PaddleOCR + 规则判定 + 智谱 GLM 初审 + 案件状态机。本 Plan 已建好的 `cases`（status=uploaded）即 AI 任务链的输入。
- Plan 3（M6–M8）：人工审核/归档/邮件通知/统计接口/最小前端。
